#!/bin/sh /etc/rc.common

START=95
STOP=10
USE_PROCD=1

PROG=/usr/libexec/airoha-gpon-config

start_service() {
	procd_open_instance
	procd_set_param command /bin/sh "$PROG" monitor
	procd_set_param respawn 3600 5 5
	procd_close_instance
}

service_triggers() {
	procd_add_reload_trigger "airoha-gpon" "airoha-epon"
}
